[package]
name = "audio"
version = "0.1.0"
edition = "2024"
description = "Real-time mono PCM capture/playback for tcast voice (cpal, no C toolchain)"

[lib]
path = "src/lib.rs"

[dependencies]
anyhow = "1.0.102"
cpal = "0.15.3"
ringbuf = "0.4.7"
tokio = { version = "1.52.3", features = ["sync"] }
